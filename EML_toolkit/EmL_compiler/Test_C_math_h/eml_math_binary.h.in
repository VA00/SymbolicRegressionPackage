#ifndef EML_MATH_BINARY_H
#define EML_MATH_BINARY_H

#include <complex.h>

static inline double complex eml(double complex x, double complex y) {
    return cexp(x) - clog(y);
}

static inline double complex @NAME@(double complex x, double complex y) {
    return @C_EXPR@;
}

#endif
